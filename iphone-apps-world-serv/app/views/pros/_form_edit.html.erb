<%= form_for @pro do |f|%>

<% if @pro.errors.any? %>
	<div id="error_explanation">
		<h2>
			<%= pluralize(@pro.errors.count, "error")%> prohibited this pro for being saved:
		</h2>
		<ul>
			<% @pro.errors.full_messages.each do |msg| %>
			<li><%= msg%></li>
			<% end %>
		</ul>
	</div>
	<% end %>


	<p>
		<%= f.label :content%><br>
		<%= f.text_area :content %>
	</p>

	<p>
		<%= f.submit%>
	</p>
	<%end%>